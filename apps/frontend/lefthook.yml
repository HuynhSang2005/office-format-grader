pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun run lint
    type-check:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun run type-check
    test:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun run test